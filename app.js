//MODULE
//dependency injection
//setup a single page app with routing to two different pages
//two controllers, two actual pages and A ROUTING to put all them together
var weatherApp = angular.module('weatherApp', ['ngRoute', 'ngResource']);

